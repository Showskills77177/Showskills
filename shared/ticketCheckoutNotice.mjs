/** Paid ticket checkout — same wording on site, Stripe, PayPal, and Terms. */
export const TICKET_PURCHASE_NON_REFUND_NOTICE =
  'Tickets are non-refundable once purchased, including if your skill answers are incorrect. No refunds after purchase.'

/** Compact checkout reminder — skill quiz + refund policy (entry modal footer). */
export const LEGACY_ENTRY_CHECKOUT_NOTICE =
  'One attempt at the three skill questions per entry. Tickets are non-refundable.'
